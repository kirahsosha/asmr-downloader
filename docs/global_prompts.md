# AI执行约束

1. 生成详细的todo list
2. 严格按照todo list的操作步骤执行
3. 忽略以下文件，文档的文本不加入分析上下文，并且禁止修改此文档内容
   1. `docs\prompts.md`
   2. `legacy-go.md`
4. `docs\wpf-migration-progress.md`文件更新约束：
   1. 忽略文件章节1.5.1到1.5.60的文本，不加入分析上下文
   2. 检查章节1.2是否需要更新
   3. 检查章节1.3是否需要更新
   4. 检查章节1.4是否需要更新
   5. 章节1.5新增变更记录，仅追加新的记录，不再修改已有历史记录
   6. 更新时需要符合章节1.6的维护规则
   7. 检查章节2.1是否需要更新
   8. 更新章节3.1的提交明细，如果存在待提交记录，合并提交明细为同一条；如果不存在，新增一条。
   9. 更新章节4，每次进行功能开发、缺陷修复或任何可能影响用户可见行为的改动时，必须先检查本章并将受影响的测试项重置为未勾选；待对应的功能测试或回归测试通过后，再重新勾选，并在必要时同步更新第 1.4 节和第 1.5 节记录。
5. 除非提示词中明确说明需要提交改动，否则只修改文件和添加待提交记录，不进行commit创建与提交
6. 如果仅有提交明细的改动，不做单独提交，与后续的代码改动一同提交。